# Token Diet

A lightweight toolkit for token compression, optimization, and budget management for LLM API calls.

## Overview
Prevent runaway API costs by compressing context, sanitizing prompts, and stripping redundant tokens before inference.
